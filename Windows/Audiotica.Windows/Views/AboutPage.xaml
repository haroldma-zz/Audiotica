<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Audiotica.Windows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Audiotica.Windows.Controls"
    x:Class="Audiotica.Windows.Views.AboutPage"
    mc:Ignorable="d"
    DataContext="{Binding AboutPage, Source={StaticResource ViewModelLocator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:PageHeader Text="About" VisualStateNarrowMinWidth="0" VisualStateNormalMinWidth="600" />
        <StackPanel Grid.Row="1" Margin="{StaticResource PageThickness}">
            <TextBlock Text="Audiotica is meant to be an all in one replacement music platform." TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>