<Page
    x:Class="Audiotica.View.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Audiotica.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <!-- material design inspired card -->
            <Grid>
                <!-- content goes here -->
                <Border Height="150" Name="CardContent" CornerRadius="5" Margin="20,10" Background="White" Padding="10"
                        Canvas.ZIndex="2">
                    <TextBlock Text="Testing card" FontSize="20" FontFamily="Segoe UI Light" />
                </Border>
                <!-- improvising a simple drop shadow -->
                <Border Height="{Binding ElementName=CardContent, Path=Height}" CornerRadius="5"
                        Margin="22,14,20,10"
                        Background="#FFC6C6C6" />
            </Grid>

            <!-- material design inspired card -->
            <Grid>
                <!-- content goes here -->
                <Border Height="200" Name="CardContent2" CornerRadius="5" Margin="20,10" Background="White"
                        Padding="10"
                        Canvas.ZIndex="2">
                    <TextBlock Text="Testing card 2" FontSize="20" FontFamily="Segoe UI Light" />
                </Border>
                <!-- improvising a simple drop shadow -->
                <Border Height="{Binding ElementName=CardContent2, Path=Height}" CornerRadius="5"
                        Margin="22,14,20,10"
                        Background="#FFC6C6C6" />
            </Grid>
        </StackPanel>
    </Grid>
</Page>