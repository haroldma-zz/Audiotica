using System;
using System.Collections.Generic;
using System.Text;
using Windows.UI.Xaml.Controls;

namespace Audiotica
{
    public static class AppBarHelper
    {
        public static void SaveState(CommandBar appBar)
        {
            
        }

        public static void RestorePreviousState(CommandBar appBar)
        {
            
        }

        public static void SwitchState(CommandBar appBar, IEnumerable<AppBarButton> primary,
            IEnumerable<AppBarButton> secondary = null)
        {
            
        }
    }
}
